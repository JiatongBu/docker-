简单尝试了一下从Docker Hub 上获取镜像
下载了Redis

Dockerfile制作镜像
Dockerfile 是一个文本文件，其内包含了一条条的 指令(Instruction)，每一条指令构建一层，因此每一条指令的内容，就是描述该层应当如何构建。

构建跨架构的Docker镜像没有成功，不知道为什么报错。



Docker容器

尝试了新建并启动容器和启动已终止容器。命令较为简单。
